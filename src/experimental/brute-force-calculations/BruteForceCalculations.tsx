function BruteForceCalculations() {
  return <div>
    <p className="text-sm">
        Trying out an approach that uses brute force calculations. Every possible input vector
        with the number elements of <code>[ -0.1, 0, 0.1]</code> is used to calculate n rounds to compare all
        results against the desired outcome vector and other requirements. 
    </p>
  </div>;
}

export default BruteForceCalculations;
